<div class="container">
  <div class="row">
    <div class="col-md-10 col-centered"></div>
    <h1> <%= @job.title %> </h1>
    <p>
      <%= simple_format(@job.description) %>
    </p>
    <div class="text-left">
      <%= link_to("回复", new_job_resume_path(@job), :style => "font-size: 30px; text-decoration: underline;") %>
    </div>
  </div>
</div>
</div>
